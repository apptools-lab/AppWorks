import React from 'react';
import MainComponent from '../src';

export default {
  // 组件名需要与 package.json 文件中的 componentConfig.name 保持一致，建议采用驼峰命名例如 BizCard 等
  name: '<%= name %>', // 组件名称
  editor: () => {
    return {
      props: [],
    };
  },
  adaptor: (props) => {
    return (
      // eslint-disable-next-line react/react-in-jsx-scope
      <MainComponent {...props} />
    );
  },
};
