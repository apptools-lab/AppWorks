import * as common from '@appworks/common-service';
import * as block from './block';
import * as project from '@appworks/project-service';
import { page, component, router, menu, material } from '@appworks/material-engine';

export default { material, common, block, component, router, menu, page, project };
